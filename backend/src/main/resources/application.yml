spring:
  datasource:
    url: jdbc:mysql://localhost:3307/scheduler
    username: scheduler
    password: scheduler
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false

  flyway:
    enabled: true

server:
  port: 8080